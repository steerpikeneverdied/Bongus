# Ranged Attack icon

Current placeholder glyph: 🏹   (tool slug: `bow`, category: `general`)
Registry keys: `fx.range`, `icon.minigame.squad`

## Where it's used in game
- Weapon VFX glyph: Range
- Minigame HUD — squad count

## What this icon should actually be
**SPLIT into separate icons** (one per use above).

Range (fx.range) should be a chibi kunai. Minigame squad-count (icon.minigame.squad) should be 3 grouped avatar silhouettes — simple circle head + shoulders, three together.
