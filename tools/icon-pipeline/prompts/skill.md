# Skill icon

Current placeholder glyph: ✨   (tool slug: `sparkle`, category: `general`)
Registry keys: `icon.ability.skill`, `icon.bestMerge`

## Where it's used in game
- Hero ability tab — skill (+ hero-menu emblem)
- Best-merge toast

## What this icon should actually be
**SPLIT into separate icons** (one per use above).

maybe just one star instead of three for ability.
Best merge toast should be a gold exclamation icon.
